<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape>
            <gradient
                android:centerColor="@color/white_low_emp"
android:endColor="@color/white_low_emp"
                android:startColor="@color/white_low_emp"
                />
            <corners android:radius="10dp" />
        </shape>
    </item>

    <!-- main color -->
    <item android:bottom="2dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/white" />
            <padding
                android:top="4dp"
                android:bottom="4dp" />
            <corners android:radius="10dp" />
        </shape>
    </item>
</layer-list>